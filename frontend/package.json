{
  "name": "code-improver",
  "displayName": "Code Improver",
  "version": "1.0.0",
  "description": "AI-powered code improvement suggestions",
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "code-improver.analyzeFile",
        "title": "Analyze Current File"
      },
      {
        "command": "code-improver.analyzeProject",
        "title": "Analyze Project"
      },
      {
        "command": "code-improver.openSettings",
        "title": "Open Settings"
      }
    ],
    "configuration": {
      "title": "Code Improver",
      "properties": {
        "codeImprover.backendUrl": {
          "type": "string",
          "default": "http://localhost:3000",
          "description": "Backend API URL"
        },
        "codeImprover.apiKey": {
          "type": "string",
          "description": "Your API Key for Code Improver"
        },
        "codeImprover.autoAnalyze": {
          "type": "boolean",
          "default": false,
          "description": "Automatically analyze files on save"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.74.0",
    "@types/node": "16.x",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "axios": "^1.5.0"
  }
}